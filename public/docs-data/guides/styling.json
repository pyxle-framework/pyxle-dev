{"title":"Styling","description":"Pyxle ships with Tailwind CSS out of the box. You can also use global CSS files, inline styles, or any CSS-in-JS solution that works with React.","html":"<p>Pyxle ships with Tailwind CSS out of the box. You can also use global CSS files, inline styles, or any CSS-in-JS solution that works with React.</p>\n<h2 id=\"tailwind-css\">Tailwind CSS</h2><p>Every scaffolded project includes Tailwind CSS pre-configured.</p>\n<h3 id=\"how-it-works\">How it works</h3><ol>\n<li><code>pages/styles/tailwind.css</code> is the Tailwind input file containing <code>@tailwind</code> directives</li>\n<li>Tailwind compiles it to <code>public/styles/tailwind.css</code></li>\n<li>The compiled CSS is linked in your page&#39;s <code>HEAD</code> variable</li>\n</ol>\n<h3 id=\"development-workflow\">Development workflow</h3><p>The <code>pyxle dev</code> command auto-starts a Tailwind watcher if it detects a Tailwind config file (<code>tailwind.config.cjs</code>, <code>tailwind.config.js</code>, etc.). You can disable this with <code>--no-tailwind</code>:</p>\n<div class=\"code-block\" data-lang=\"bash\"><pre><code class=\"language-bash\">pyxle dev              # Tailwind watcher starts automatically\npyxle dev --no-tailwind  # Skip Tailwind watcher</code></pre></div><p>Alternatively, run the Tailwind watcher in a separate terminal:</p>\n<div class=\"code-block\" data-lang=\"bash\"><pre><code class=\"language-bash\">npm run dev:css</code></pre></div><h3 id=\"production-builds\">Production builds</h3><p><code>pyxle build</code> runs <code>npm run build</code>, which triggers <code>npm run build:css</code> before the Vite bundle. The minified CSS ends up in your production output.</p>\n<h3 id=\"configuration\">Configuration</h3><p>The scaffold generates a <code>tailwind.config.cjs</code> that scans <code>.pyx</code> and <code>.jsx</code> files:</p>\n<div class=\"code-block\" data-lang=\"javascript\"><pre><code class=\"language-javascript\">module.exports = {\n  content: ['./pages/**/*.{pyx,jsx,js,tsx,ts}'],\n  darkMode: 'class',\n  theme: {\n    extend: {},\n  },\n  plugins: [\n    require('@tailwindcss/forms'),\n    require('@tailwindcss/typography'),\n  ],\n};</code></pre></div><h3 id=\"dark-mode\">Dark mode</h3><p>The scaffold includes a theme toggle script that reads from <code>localStorage</code> and respects <code>prefers-color-scheme</code>. Toggle the <code>dark</code> class on <code>&lt;html&gt;</code> to switch themes:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">function toggleTheme() {\n  document.documentElement.classList.toggle('dark');\n}</code></pre></div><p>Use Tailwind&#39;s <code>dark:</code> modifier for dark-mode styles:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">&lt;div className=\"bg-white dark:bg-slate-900 text-black dark:text-white\"&gt;\n  Content\n&lt;/div&gt;</code></pre></div><h2 id=\"global-stylesheets\">Global stylesheets</h2><p>Register CSS files that should be inlined on every page:</p>\n<div class=\"code-block\" data-lang=\"json\"><pre><code class=\"language-json\">{\n  \"styling\": {\n    \"globalStyles\": [\n      \"styles/reset.css\",\n      \"styles/typography.css\"\n    ]\n  }\n}</code></pre></div><p>Paths are relative to the project root. Global styles are loaded in order and inlined into the SSR HTML as <code>&lt;style&gt;</code> tags, so they work even before JavaScript loads.</p>\n<h2 id=\"global-scripts\">Global scripts</h2><p>Register JavaScript files loaded on every page:</p>\n<div class=\"code-block\" data-lang=\"json\"><pre><code class=\"language-json\">{\n  \"styling\": {\n    \"globalScripts\": [\n      \"scripts/analytics.js\"\n    ]\n  }\n}</code></pre></div><h2 id=\"linking-a-stylesheet-in-head\">Linking a stylesheet in HEAD</h2><p>For external or compiled stylesheets, link them in the <code>HEAD</code> variable:</p>\n<div class=\"code-block\" data-lang=\"python\"><pre><code class=\"language-python\">HEAD = '&lt;link rel=\"stylesheet\" href=\"/styles/tailwind.css\" /&gt;'</code></pre></div><p>Or in a layout&#39;s JSX:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">import { Head } from 'pyxle/client';\n\nexport default function Layout({ children }) {\n  return (\n    &lt;&gt;\n      &lt;Head&gt;\n        &lt;link rel=\"stylesheet\" href=\"/styles/tailwind.css\" /&gt;\n      &lt;/Head&gt;\n      {children}\n    &lt;/&gt;\n  );\n}</code></pre></div><h2 id=\"css-in-js\">CSS-in-JS</h2><p>Any CSS-in-JS library that works with React 18 and SSR should work with Pyxle. Install it via npm and import it in your JSX section.</p>\n<h2 id=\"next-steps\">Next steps</h2><ul>\n<li>Manage document head elements: <a href=\"/docs/guides/head-management\">Head Management</a></li>\n<li>Add scripts with loading strategies: <a href=\"/docs/guides/client-components\">Client Components</a></li>\n</ul>\n","toc":[{"depth":2,"text":"Tailwind CSS","slug":"tailwind-css"},{"depth":3,"text":"How it works","slug":"how-it-works"},{"depth":3,"text":"Development workflow","slug":"development-workflow"},{"depth":3,"text":"Production builds","slug":"production-builds"},{"depth":3,"text":"Configuration","slug":"configuration"},{"depth":3,"text":"Dark mode","slug":"dark-mode"},{"depth":2,"text":"Global stylesheets","slug":"global-stylesheets"},{"depth":2,"text":"Global scripts","slug":"global-scripts"},{"depth":2,"text":"Linking a stylesheet in HEAD","slug":"linking-a-stylesheet-in-head"},{"depth":2,"text":"CSS-in-JS","slug":"css-in-js"},{"depth":2,"text":"Next steps","slug":"next-steps"}],"path":"guides/styling","markdown":"# Styling\n\nPyxle ships with Tailwind CSS out of the box. You can also use global CSS files, inline styles, or any CSS-in-JS solution that works with React.\n\n## Tailwind CSS\n\nEvery scaffolded project includes Tailwind CSS pre-configured.\n\n### How it works\n\n1. `pages/styles/tailwind.css` is the Tailwind input file containing `@tailwind` directives\n2. Tailwind compiles it to `public/styles/tailwind.css`\n3. The compiled CSS is linked in your page's `HEAD` variable\n\n### Development workflow\n\nThe `pyxle dev` command auto-starts a Tailwind watcher if it detects a Tailwind config file (`tailwind.config.cjs`, `tailwind.config.js`, etc.). You can disable this with `--no-tailwind`:\n\n```bash\npyxle dev              # Tailwind watcher starts automatically\npyxle dev --no-tailwind  # Skip Tailwind watcher\n```\n\nAlternatively, run the Tailwind watcher in a separate terminal:\n\n```bash\nnpm run dev:css\n```\n\n### Production builds\n\n`pyxle build` runs `npm run build`, which triggers `npm run build:css` before the Vite bundle. The minified CSS ends up in your production output.\n\n### Configuration\n\nThe scaffold generates a `tailwind.config.cjs` that scans `.pyx` and `.jsx` files:\n\n```javascript\nmodule.exports = {\n  content: ['./pages/**/*.{pyx,jsx,js,tsx,ts}'],\n  darkMode: 'class',\n  theme: {\n    extend: {},\n  },\n  plugins: [\n    require('@tailwindcss/forms'),\n    require('@tailwindcss/typography'),\n  ],\n};\n```\n\n### Dark mode\n\nThe scaffold includes a theme toggle script that reads from `localStorage` and respects `prefers-color-scheme`. Toggle the `dark` class on `<html>` to switch themes:\n\n```jsx\nfunction toggleTheme() {\n  document.documentElement.classList.toggle('dark');\n}\n```\n\nUse Tailwind's `dark:` modifier for dark-mode styles:\n\n```jsx\n<div className=\"bg-white dark:bg-slate-900 text-black dark:text-white\">\n  Content\n</div>\n```\n\n## Global stylesheets\n\nRegister CSS files that should be inlined on every page:\n\n```json\n{\n  \"styling\": {\n    \"globalStyles\": [\n      \"styles/reset.css\",\n      \"styles/typography.css\"\n    ]\n  }\n}\n```\n\nPaths are relative to the project root. Global styles are loaded in order and inlined into the SSR HTML as `<style>` tags, so they work even before JavaScript loads.\n\n## Global scripts\n\nRegister JavaScript files loaded on every page:\n\n```json\n{\n  \"styling\": {\n    \"globalScripts\": [\n      \"scripts/analytics.js\"\n    ]\n  }\n}\n```\n\n## Linking a stylesheet in HEAD\n\nFor external or compiled stylesheets, link them in the `HEAD` variable:\n\n```python\nHEAD = '<link rel=\"stylesheet\" href=\"/styles/tailwind.css\" />'\n```\n\nOr in a layout's JSX:\n\n```jsx\nimport { Head } from 'pyxle/client';\n\nexport default function Layout({ children }) {\n  return (\n    <>\n      <Head>\n        <link rel=\"stylesheet\" href=\"/styles/tailwind.css\" />\n      </Head>\n      {children}\n    </>\n  );\n}\n```\n\n## CSS-in-JS\n\nAny CSS-in-JS library that works with React 18 and SSR should work with Pyxle. Install it via npm and import it in your JSX section.\n\n## Next steps\n\n- Manage document head elements: [Head Management](head-management.md)\n- Add scripts with loading strategies: [Client Components](client-components.md)\n","prev":{"title":"Layouts","path":"core-concepts/layouts"},"next":{"title":"Head Management","path":"guides/head-management"}}