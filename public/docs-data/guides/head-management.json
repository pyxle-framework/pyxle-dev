{"title":"Head Management","description":"Pyxle offers two ways to control the document `<head>`: the `<Head>` component (**recommended**) and the `HEAD` Python variable. Both merge together with automatic deduplication, and either works — bu","html":"<p>Pyxle offers two ways to control the document <code>&lt;head&gt;</code>: the <code>&lt;Head&gt;</code> component (<strong>recommended</strong>) and the <code>HEAD</code> Python variable. Both merge together with automatic deduplication, and either works — but we recommend <code>&lt;Head&gt;</code> for almost every real page.</p>\n<h2 id=\"tldr-use-the-head-component\">TL;DR — use the <code>&lt;Head&gt;</code> component</h2><div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">import { Head } from 'pyxle/client';\n\nexport default function Page({ data }) {\n  return (\n    &lt;&gt;\n      &lt;Head&gt;\n        &lt;title&gt;{data.post.title} — My Blog&lt;/title&gt;\n        &lt;meta name=\"description\" content={data.post.excerpt} /&gt;\n        &lt;link rel=\"canonical\" href={`https://example.com/posts/${data.post.slug}`} /&gt;\n      &lt;/Head&gt;\n      &lt;article&gt;\n        &lt;h1&gt;{data.post.title}&lt;/h1&gt;\n        {/* ... */}\n      &lt;/article&gt;\n    &lt;/&gt;\n  );\n}</code></pre></div><p>That&#39;s the pattern you&#39;ll use for the vast majority of pages. It reads like React, interpolates props naturally, and lives next to the body markup that depends on the same data.</p>\n<h2 id=\"why-head-is-the-recommended-approach\">Why <code>&lt;Head&gt;</code> is the recommended approach</h2><ul>\n<li><strong>It&#39;s just JSX.</strong> If you know React, you already know how to use it. No new concepts, no callable-vs-string rules to memorise.</li>\n<li><strong>Dynamic content is effortless.</strong> You can interpolate <code>{data.foo}</code>, map over arrays, use conditionals, extract to subcomponents — everything JSX lets you do with the body works in the head too.</li>\n<li><strong>Colocation.</strong> Your <code>&lt;title&gt;</code> sits right next to the <code>&lt;h1&gt;</code> that uses the same data. Refactoring one updates the other in the same diff.</li>\n<li><strong>Works in nested components.</strong> Any component in your render tree can contribute head elements. A <code>&lt;BlogPostCard&gt;</code> component can set its own <code>og:image</code>; an <code>&lt;AdminOnly&gt;</code> wrapper can add <code>&lt;meta name=&quot;robots&quot; content=&quot;noindex&quot; /&gt;</code>.</li>\n<li><strong>Plays well with layouts.</strong> Layouts can set defaults with their own <code>&lt;Head&gt;</code>, and pages override them automatically through Pyxle&#39;s deduplication rules.</li>\n<li><strong>Familiar to developers coming from other frameworks.</strong> The <code>&lt;Head&gt;</code> API is intentionally similar to Next.js&#39;s <code>next/head</code>, Remix&#39;s <code>Meta</code> export, and React Helmet.</li>\n</ul>\n<h2 id=\"the-head-component\">The <code>&lt;Head&gt;</code> component</h2><p>Import it from <code>pyxle/client</code> and drop it anywhere in your component tree. Its children become elements in the document <code>&lt;head&gt;</code>:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">import { Head } from 'pyxle/client';\n\nexport default function Page({ data }) {\n  return (\n    &lt;&gt;\n      &lt;Head&gt;\n        &lt;title&gt;{data.title}&lt;/title&gt;\n        &lt;meta name=\"description\" content={data.description} /&gt;\n        &lt;meta name=\"robots\" content=\"noindex\" /&gt;\n        &lt;link rel=\"canonical\" href={data.canonicalUrl} /&gt;\n      &lt;/Head&gt;\n      &lt;h1&gt;{data.title}&lt;/h1&gt;\n    &lt;/&gt;\n  );\n}</code></pre></div><p>The <code>&lt;Head&gt;</code> component:</p>\n<ul>\n<li><strong>Renders nothing in the DOM</strong> (it returns <code>null</code>).</li>\n<li>During SSR, Pyxle extracts its children at compile time and registers them as head elements for the response.</li>\n<li><strong>Works in any component</strong>, including nested ones. A reusable component can inject its own head metadata.</li>\n<li><strong>Supports any head-valid element</strong>: <code>&lt;title&gt;</code>, <code>&lt;meta&gt;</code>, <code>&lt;link&gt;</code>, <code>&lt;script&gt;</code>, <code>&lt;base&gt;</code>.</li>\n<li><strong>Normalises multi-part <code>&lt;title&gt;</code> children</strong> since 0.3.0. <code>&lt;title&gt;{name} — My Blog&lt;/title&gt;</code> compiles to multiple children — <code>[name, &quot; — My Blog&quot;]</code> — which React warns about. <code>&lt;Head&gt;</code> joins string and number children into a single text node so the warning is silenced and the rendered HTML is unchanged. You don&#39;t need template literals or <code>{ \\</code>${name} — My Blog` }` workarounds.</li>\n</ul>\n<h3 id=\"multiple-head-blocks-in-one-tree\">Multiple <code>&lt;Head&gt;</code> blocks in one tree</h3><p>You can use <code>&lt;Head&gt;</code> multiple times — it&#39;s not a singleton. Elements from all <code>&lt;Head&gt;</code> blocks are collected and merged:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">export default function BlogPost({ data }) {\n  return (\n    &lt;article&gt;\n      &lt;Head&gt;\n        &lt;title&gt;{data.post.title}&lt;/title&gt;\n      &lt;/Head&gt;\n      &lt;Header /&gt;\n      &lt;PostBody post={data.post} /&gt;\n      {data.post.isPremium &amp;&amp; (\n        &lt;Head&gt;\n          &lt;meta name=\"robots\" content=\"noindex\" /&gt;\n        &lt;/Head&gt;\n      )}\n    &lt;/article&gt;\n  );\n}</code></pre></div><p>This lets you put head contributions close to the code that decides them, without having to lift that logic all the way up to the page root.</p>\n<h3 id=\"using-head-in-reusable-components\">Using <code>&lt;Head&gt;</code> in reusable components</h3><p>Head contributions from any rendered component get merged into the final document:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">// components/SeoTags.jsx\nimport { Head } from 'pyxle/client';\n\nexport function SeoTags({ title, description, image }) {\n  return (\n    &lt;Head&gt;\n      &lt;title&gt;{title}&lt;/title&gt;\n      &lt;meta name=\"description\" content={description} /&gt;\n      &lt;meta property=\"og:title\" content={title} /&gt;\n      &lt;meta property=\"og:description\" content={description} /&gt;\n      &lt;meta property=\"og:image\" content={image} /&gt;\n      &lt;meta name=\"twitter:card\" content=\"summary_large_image\" /&gt;\n    &lt;/Head&gt;\n  );\n}\n\n// pages/blog/[slug].pyxl\nimport { SeoTags } from '../../components/SeoTags.jsx';\n\nexport default function BlogPost({ data }) {\n  return (\n    &lt;article&gt;\n      &lt;SeoTags\n        title={data.post.title}\n        description={data.post.excerpt}\n        image={data.post.coverImage}\n      /&gt;\n      &lt;h1&gt;{data.post.title}&lt;/h1&gt;\n      {/* ... */}\n    &lt;/article&gt;\n  );\n}</code></pre></div><p>This is the idiomatic way to build SEO presets, third-party tracking tags, and theme toggles.</p>\n<h2 id=\"the-head-variable-lower-level-alternative\">The <code>HEAD</code> variable (lower-level alternative)</h2><p><code>.pyxl</code> files can also define a <code>HEAD</code> variable in the Python section. This was Pyxle&#39;s original head mechanism and still works; the parser extracts it at compile time, before React is involved.</p>\n<div class=\"code-block\" data-lang=\"python\"><pre><code class=\"language-python\"># Static string\nHEAD = '&lt;title&gt;My Page&lt;/title&gt;&lt;meta name=\"description\" content=\"Page description\" /&gt;'\n\n# Or a list of strings\nHEAD = [\n    '&lt;title&gt;My Page&lt;/title&gt;',\n    '&lt;meta name=\"description\" content=\"Page description\" /&gt;',\n    '&lt;link rel=\"canonical\" href=\"https://example.com/page\" /&gt;',\n]</code></pre></div><p>For head content that depends on loader data, use a callable:</p>\n<div class=\"code-block\" data-lang=\"python\"><pre><code class=\"language-python\">@server\nasync def load_post(request):\n    post = await fetch_post(request.path_params[\"slug\"])\n    return {\"post\": post}\n\ndef HEAD(data):\n    return [\n        f'&lt;title&gt;{data[\"post\"][\"title\"]} - My Blog&lt;/title&gt;',\n        f'&lt;meta name=\"description\" content=\"{data[\"post\"][\"excerpt\"]}\" /&gt;',\n    ]</code></pre></div><p>The callable receives the loader&#39;s return value as its argument and must return a string or list of strings.</p>\n<h3 id=\"when-to-prefer-the-head-variable\">When to prefer the <code>HEAD</code> variable</h3><p>There are a few narrow cases where the <code>HEAD</code> variable is a better fit than <code>&lt;Head&gt;</code>:</p>\n<ol>\n<li><strong>Pages with no React component.</strong> A pure API-like page that returns minimal HTML and wants a static <code>&lt;title&gt;</code> without any client-side JavaScript.</li>\n<li><strong>Absolute hot paths</strong> where the few microseconds of skipping React&#39;s head capture matter, and the content is fully static. In practice this matters for approximately no one.</li>\n<li><strong>You deliberately want the head to be decoupled from the render tree</strong> — for example, if a page&#39;s head is determined by something the component doesn&#39;t need to know about.</li>\n</ol>\n<p>Unless one of these applies, reach for <code>&lt;Head&gt;</code>.</p>\n<h3 id=\"xss-safety\">XSS safety</h3><p>Both <code>&lt;Head&gt;</code> children and <code>HEAD</code> strings are automatically sanitised:</p>\n<ul>\n<li>Angle brackets (<code>&lt;</code>, <code>&gt;</code>) inside <code>&lt;title&gt;</code> text are escaped</li>\n<li>Event handler attributes (<code>onclick</code>, <code>onerror</code>, etc.) are stripped</li>\n<li><code>javascript:</code> and <code>vbscript:</code> URLs in <code>href</code>/<code>src</code> attributes are removed</li>\n</ul>\n<p>This protects against XSS when interpolating user-provided data into head elements. You should still escape user input as a best practice.</p>\n<h2 id=\"layouts-and-precedence\">Layouts and precedence</h2><p>When multiple sources define the same head element, Pyxle deduplicates them. Later sources override earlier ones.</p>\n<h3 id=\"precedence-order-lowest-to-highest\">Precedence order (lowest to highest)</h3><ol>\n<li>Layout <code>&lt;Head&gt;</code> blocks and layout <code>HEAD</code> variable</li>\n<li>Page <code>HEAD</code> variable</li>\n<li>Page <code>&lt;Head&gt;</code> blocks</li>\n</ol>\n<p>Within the same tier, deeper nesting wins (a <code>&lt;Head&gt;</code> in a child component overrides a <code>&lt;Head&gt;</code> in a parent component).</p>\n<h3 id=\"example-layout-defaults-page-overrides\">Example: layout defaults + page overrides</h3><div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">// pages/layout.pyxl\nimport { Head } from 'pyxle/client';\n\nexport default function RootLayout({ children }) {\n  return (\n    &lt;html lang=\"en\"&gt;\n      &lt;Head&gt;\n        &lt;title&gt;My Site&lt;/title&gt;\n        &lt;meta name=\"description\" content=\"The default description for My Site.\" /&gt;\n        &lt;link rel=\"icon\" href=\"/favicon.ico\" /&gt;\n      &lt;/Head&gt;\n      &lt;body&gt;{children}&lt;/body&gt;\n    &lt;/html&gt;\n  );\n}</code></pre></div><div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">// pages/about.pyxl\nimport { Head } from 'pyxle/client';\n\nexport default function About() {\n  return (\n    &lt;&gt;\n      &lt;Head&gt;\n        &lt;title&gt;About — My Site&lt;/title&gt;\n        &lt;meta name=\"description\" content=\"The story behind My Site.\" /&gt;\n      &lt;/Head&gt;\n      &lt;h1&gt;About&lt;/h1&gt;\n    &lt;/&gt;\n  );\n}</code></pre></div><p>When the <code>/about</code> route renders, the layout&#39;s <code>&lt;title&gt;My Site&lt;/title&gt;</code> and its description are both overridden by the page&#39;s values. The favicon <code>&lt;link&gt;</code> survives because the page doesn&#39;t define one.</p>\n<h3 id=\"deduplication-rules\">Deduplication rules</h3><table>\n<thead>\n<tr>\n<th>Element</th>\n<th>Deduplicated by</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&lt;title&gt;</code></td>\n<td>Tag name (only one title allowed)</td>\n</tr>\n<tr>\n<td><code>&lt;meta name=&quot;X&quot;&gt;</code></td>\n<td>The <code>name</code> attribute</td>\n</tr>\n<tr>\n<td><code>&lt;meta property=&quot;X&quot;&gt;</code></td>\n<td>The <code>property</code> attribute</td>\n</tr>\n<tr>\n<td><code>&lt;meta charset&gt;</code></td>\n<td>Always one charset</td>\n</tr>\n<tr>\n<td><code>&lt;link rel=&quot;canonical&quot;&gt;</code></td>\n<td>Only one canonical</td>\n</tr>\n<tr>\n<td><code>&lt;link rel=&quot;X&quot; href=&quot;Y&quot;&gt;</code></td>\n<td><code>rel</code> + <code>href</code> combination</td>\n</tr>\n<tr>\n<td><code>&lt;script src=&quot;X&quot;&gt;</code></td>\n<td>The <code>src</code> attribute</td>\n</tr>\n<tr>\n<td>Elements with <code>data-head-key=&quot;X&quot;</code></td>\n<td>The key value</td>\n</tr>\n<tr>\n<td>Everything else</td>\n<td>Not deduplicated (all instances kept)</td>\n</tr>\n</tbody></table>\n<h3 id=\"manual-deduplication-keys\">Manual deduplication keys</h3><p>Use <code>data-head-key</code> to control deduplication for custom elements that don&#39;t have a natural identity attribute:</p>\n<div class=\"code-block\" data-lang=\"jsx\"><pre><code class=\"language-jsx\">&lt;Head&gt;\n  &lt;script src=\"/analytics.js\" data-head-key=\"analytics\"&gt;&lt;/script&gt;\n&lt;/Head&gt;</code></pre></div><p>If a layout and a page both define an element with the same <code>data-head-key</code>, the higher-priority source wins. This is useful for tag managers, feature-flag bootstrap scripts, or A/B testing snippets.</p>\n<h2 id=\"default-title\">Default title</h2><p>If no <code>&lt;title&gt;</code> element is provided by any source, Pyxle inserts a default:</p>\n<div class=\"code-block\" data-lang=\"html\"><pre><code class=\"language-html\">&lt;title&gt;Pyxle&lt;/title&gt;</code></pre></div><h2 id=\"next-steps\">Next steps</h2><ul>\n<li>Add third-party scripts: <a href=\"/docs/guides/client-components\">Client Components</a></li>\n<li>Build JSON APIs: <a href=\"/docs/guides/api-routes\">API Routes</a></li>\n</ul>\n","toc":[{"depth":2,"text":"TL;DR — use the <Head> component","slug":"tldr-use-the-head-component"},{"depth":2,"text":"Why <Head> is the recommended approach","slug":"why-head-is-the-recommended-approach"},{"depth":2,"text":"The <Head> component","slug":"the-head-component"},{"depth":3,"text":"Multiple <Head> blocks in one tree","slug":"multiple-head-blocks-in-one-tree"},{"depth":3,"text":"Using <Head> in reusable components","slug":"using-head-in-reusable-components"},{"depth":2,"text":"The HEAD variable (lower-level alternative)","slug":"the-head-variable-lower-level-alternative"},{"depth":3,"text":"When to prefer the HEAD variable","slug":"when-to-prefer-the-head-variable"},{"depth":3,"text":"XSS safety","slug":"xss-safety"},{"depth":2,"text":"Layouts and precedence","slug":"layouts-and-precedence"},{"depth":3,"text":"Precedence order (lowest to highest)","slug":"precedence-order-lowest-to-highest"},{"depth":3,"text":"Example: layout defaults + page overrides","slug":"example-layout-defaults-page-overrides"},{"depth":3,"text":"Deduplication rules","slug":"deduplication-rules"},{"depth":3,"text":"Manual deduplication keys","slug":"manual-deduplication-keys"},{"depth":2,"text":"Default title","slug":"default-title"},{"depth":2,"text":"Next steps","slug":"next-steps"}],"path":"guides/head-management","markdown":"# Head Management\n\nPyxle offers two ways to control the document `<head>`: the `<Head>` component (**recommended**) and the `HEAD` Python variable. Both merge together with automatic deduplication, and either works — but we recommend `<Head>` for almost every real page.\n\n## TL;DR — use the `<Head>` component\n\n```jsx\nimport { Head } from 'pyxle/client';\n\nexport default function Page({ data }) {\n  return (\n    <>\n      <Head>\n        <title>{data.post.title} — My Blog</title>\n        <meta name=\"description\" content={data.post.excerpt} />\n        <link rel=\"canonical\" href={`https://example.com/posts/${data.post.slug}`} />\n      </Head>\n      <article>\n        <h1>{data.post.title}</h1>\n        {/* ... */}\n      </article>\n    </>\n  );\n}\n```\n\nThat's the pattern you'll use for the vast majority of pages. It reads like React, interpolates props naturally, and lives next to the body markup that depends on the same data.\n\n## Why `<Head>` is the recommended approach\n\n- **It's just JSX.** If you know React, you already know how to use it. No new concepts, no callable-vs-string rules to memorise.\n- **Dynamic content is effortless.** You can interpolate `{data.foo}`, map over arrays, use conditionals, extract to subcomponents — everything JSX lets you do with the body works in the head too.\n- **Colocation.** Your `<title>` sits right next to the `<h1>` that uses the same data. Refactoring one updates the other in the same diff.\n- **Works in nested components.** Any component in your render tree can contribute head elements. A `<BlogPostCard>` component can set its own `og:image`; an `<AdminOnly>` wrapper can add `<meta name=\"robots\" content=\"noindex\" />`.\n- **Plays well with layouts.** Layouts can set defaults with their own `<Head>`, and pages override them automatically through Pyxle's deduplication rules.\n- **Familiar to developers coming from other frameworks.** The `<Head>` API is intentionally similar to Next.js's `next/head`, Remix's `Meta` export, and React Helmet.\n\n## The `<Head>` component\n\nImport it from `pyxle/client` and drop it anywhere in your component tree. Its children become elements in the document `<head>`:\n\n```jsx\nimport { Head } from 'pyxle/client';\n\nexport default function Page({ data }) {\n  return (\n    <>\n      <Head>\n        <title>{data.title}</title>\n        <meta name=\"description\" content={data.description} />\n        <meta name=\"robots\" content=\"noindex\" />\n        <link rel=\"canonical\" href={data.canonicalUrl} />\n      </Head>\n      <h1>{data.title}</h1>\n    </>\n  );\n}\n```\n\nThe `<Head>` component:\n\n- **Renders nothing in the DOM** (it returns `null`).\n- During SSR, Pyxle extracts its children at compile time and registers them as head elements for the response.\n- **Works in any component**, including nested ones. A reusable component can inject its own head metadata.\n- **Supports any head-valid element**: `<title>`, `<meta>`, `<link>`, `<script>`, `<base>`.\n- **Normalises multi-part `<title>` children** since 0.3.0. `<title>{name} — My Blog</title>` compiles to multiple children — `[name, \" — My Blog\"]` — which React warns about. `<Head>` joins string and number children into a single text node so the warning is silenced and the rendered HTML is unchanged. You don't need template literals or `{ \\`${name} — My Blog\\` }` workarounds.\n\n### Multiple `<Head>` blocks in one tree\n\nYou can use `<Head>` multiple times — it's not a singleton. Elements from all `<Head>` blocks are collected and merged:\n\n```jsx\nexport default function BlogPost({ data }) {\n  return (\n    <article>\n      <Head>\n        <title>{data.post.title}</title>\n      </Head>\n      <Header />\n      <PostBody post={data.post} />\n      {data.post.isPremium && (\n        <Head>\n          <meta name=\"robots\" content=\"noindex\" />\n        </Head>\n      )}\n    </article>\n  );\n}\n```\n\nThis lets you put head contributions close to the code that decides them, without having to lift that logic all the way up to the page root.\n\n### Using `<Head>` in reusable components\n\nHead contributions from any rendered component get merged into the final document:\n\n```jsx\n// components/SeoTags.jsx\nimport { Head } from 'pyxle/client';\n\nexport function SeoTags({ title, description, image }) {\n  return (\n    <Head>\n      <title>{title}</title>\n      <meta name=\"description\" content={description} />\n      <meta property=\"og:title\" content={title} />\n      <meta property=\"og:description\" content={description} />\n      <meta property=\"og:image\" content={image} />\n      <meta name=\"twitter:card\" content=\"summary_large_image\" />\n    </Head>\n  );\n}\n\n// pages/blog/[slug].pyxl\nimport { SeoTags } from '../../components/SeoTags.jsx';\n\nexport default function BlogPost({ data }) {\n  return (\n    <article>\n      <SeoTags\n        title={data.post.title}\n        description={data.post.excerpt}\n        image={data.post.coverImage}\n      />\n      <h1>{data.post.title}</h1>\n      {/* ... */}\n    </article>\n  );\n}\n```\n\nThis is the idiomatic way to build SEO presets, third-party tracking tags, and theme toggles.\n\n## The `HEAD` variable (lower-level alternative)\n\n`.pyxl` files can also define a `HEAD` variable in the Python section. This was Pyxle's original head mechanism and still works; the parser extracts it at compile time, before React is involved.\n\n```python\n# Static string\nHEAD = '<title>My Page</title><meta name=\"description\" content=\"Page description\" />'\n\n# Or a list of strings\nHEAD = [\n    '<title>My Page</title>',\n    '<meta name=\"description\" content=\"Page description\" />',\n    '<link rel=\"canonical\" href=\"https://example.com/page\" />',\n]\n```\n\nFor head content that depends on loader data, use a callable:\n\n```python\n@server\nasync def load_post(request):\n    post = await fetch_post(request.path_params[\"slug\"])\n    return {\"post\": post}\n\ndef HEAD(data):\n    return [\n        f'<title>{data[\"post\"][\"title\"]} - My Blog</title>',\n        f'<meta name=\"description\" content=\"{data[\"post\"][\"excerpt\"]}\" />',\n    ]\n```\n\nThe callable receives the loader's return value as its argument and must return a string or list of strings.\n\n### When to prefer the `HEAD` variable\n\nThere are a few narrow cases where the `HEAD` variable is a better fit than `<Head>`:\n\n1. **Pages with no React component.** A pure API-like page that returns minimal HTML and wants a static `<title>` without any client-side JavaScript.\n2. **Absolute hot paths** where the few microseconds of skipping React's head capture matter, and the content is fully static. In practice this matters for approximately no one.\n3. **You deliberately want the head to be decoupled from the render tree** — for example, if a page's head is determined by something the component doesn't need to know about.\n\nUnless one of these applies, reach for `<Head>`.\n\n### XSS safety\n\nBoth `<Head>` children and `HEAD` strings are automatically sanitised:\n\n- Angle brackets (`<`, `>`) inside `<title>` text are escaped\n- Event handler attributes (`onclick`, `onerror`, etc.) are stripped\n- `javascript:` and `vbscript:` URLs in `href`/`src` attributes are removed\n\nThis protects against XSS when interpolating user-provided data into head elements. You should still escape user input as a best practice.\n\n## Layouts and precedence\n\nWhen multiple sources define the same head element, Pyxle deduplicates them. Later sources override earlier ones.\n\n### Precedence order (lowest to highest)\n\n1. Layout `<Head>` blocks and layout `HEAD` variable\n2. Page `HEAD` variable\n3. Page `<Head>` blocks\n\nWithin the same tier, deeper nesting wins (a `<Head>` in a child component overrides a `<Head>` in a parent component).\n\n### Example: layout defaults + page overrides\n\n```jsx\n// pages/layout.pyxl\nimport { Head } from 'pyxle/client';\n\nexport default function RootLayout({ children }) {\n  return (\n    <html lang=\"en\">\n      <Head>\n        <title>My Site</title>\n        <meta name=\"description\" content=\"The default description for My Site.\" />\n        <link rel=\"icon\" href=\"/favicon.ico\" />\n      </Head>\n      <body>{children}</body>\n    </html>\n  );\n}\n```\n\n```jsx\n// pages/about.pyxl\nimport { Head } from 'pyxle/client';\n\nexport default function About() {\n  return (\n    <>\n      <Head>\n        <title>About — My Site</title>\n        <meta name=\"description\" content=\"The story behind My Site.\" />\n      </Head>\n      <h1>About</h1>\n    </>\n  );\n}\n```\n\nWhen the `/about` route renders, the layout's `<title>My Site</title>` and its description are both overridden by the page's values. The favicon `<link>` survives because the page doesn't define one.\n\n### Deduplication rules\n\n| Element | Deduplicated by |\n|---------|----------------|\n| `<title>` | Tag name (only one title allowed) |\n| `<meta name=\"X\">` | The `name` attribute |\n| `<meta property=\"X\">` | The `property` attribute |\n| `<meta charset>` | Always one charset |\n| `<link rel=\"canonical\">` | Only one canonical |\n| `<link rel=\"X\" href=\"Y\">` | `rel` + `href` combination |\n| `<script src=\"X\">` | The `src` attribute |\n| Elements with `data-head-key=\"X\"` | The key value |\n| Everything else | Not deduplicated (all instances kept) |\n\n### Manual deduplication keys\n\nUse `data-head-key` to control deduplication for custom elements that don't have a natural identity attribute:\n\n```jsx\n<Head>\n  <script src=\"/analytics.js\" data-head-key=\"analytics\"></script>\n</Head>\n```\n\nIf a layout and a page both define an element with the same `data-head-key`, the higher-priority source wins. This is useful for tag managers, feature-flag bootstrap scripts, or A/B testing snippets.\n\n## Default title\n\nIf no `<title>` element is provided by any source, Pyxle inserts a default:\n\n```html\n<title>Pyxle</title>\n```\n\n## Next steps\n\n- Add third-party scripts: [Client Components](client-components.md)\n- Build JSON APIs: [API Routes](api-routes.md)\n","prev":{"title":"Styling","path":"guides/styling"},"next":{"title":"API Routes","path":"guides/api-routes"}}