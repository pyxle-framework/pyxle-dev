{"title":"pyxle-db","description":"A SQLite-first database plugin for Pyxle:","html":"<blockquote>\n<p>🚧 <strong>Coming soon — not yet released.</strong></p>\n<p><code>pyxle-db</code> is being developed in internal beta and is <strong>not</strong> available on PyPI yet. There is nothing to <code>pip install</code> today. This page is a placeholder so we can link to it from the plugin docs once the package ships.</p>\n</blockquote>\n<h2 id=\"what-it-will-be\">What it will be</h2><p>A SQLite-first database plugin for Pyxle:</p>\n<ul>\n<li><p>A single <code>Database</code> instance shared across loaders and actions, registered through the <a href=\"/docs/guides/plugins\">plugin system</a>.</p>\n</li>\n<li><p>Connection pooling and PRAGMA hardening tuned for the Pyxle request lifecycle.</p>\n</li>\n<li><p>Filesystem-driven migrations (<code>migrations/0001-init.sql</code>, <code>0002-…</code>) with checksum tracking so a committed migration can&#39;t be silently edited.</p>\n</li>\n<li><p>A <code>get_database()</code> shortcut so app code stays terse:</p>\n<div class=\"code-block\" data-lang=\"python\"><pre><code class=\"language-python\">from pyxle_db import get_database\n\n@server\nasync def load(request):\n    db = get_database()\n    row = await db.fetchone(\"SELECT id, title FROM posts WHERE id = ?\", (request.path_params[\"id\"],))\n    return {\"post\": dict(row) if row else None}</code></pre></div></li>\n</ul>\n<h2 id=\"status\">Status</h2><ul>\n<li><strong>Source</strong>: internal repo — not public yet.</li>\n<li><strong>PyPI</strong>: not published.</li>\n<li><strong>ETA</strong>: very soon. Subscribe on <a href=\"https://pyxle.dev\" target=\"_blank\" rel=\"noreferrer\">pyxle.dev</a> to be notified when it lands.</li>\n</ul>\n<h2 id=\"see-also\">See also</h2><ul>\n<li><a href=\"/docs/guides/plugins\">Plugins guide</a> — plugin system overview.</li>\n<li><a href=\"/docs/reference/plugins-api\">Plugins API reference</a> — the API third-party plugins build on today.</li>\n</ul>\n","toc":[{"depth":2,"text":"What it will be","slug":"what-it-will-be"},{"depth":2,"text":"Status","slug":"status"},{"depth":2,"text":"See also","slug":"see-also"}],"path":"plugins/pyxle-db","markdown":"# pyxle-db\n\n> 🚧 **Coming soon — not yet released.**\n>\n> `pyxle-db` is being developed in internal beta and is **not** available on PyPI yet. There is nothing to `pip install` today. This page is a placeholder so we can link to it from the plugin docs once the package ships.\n\n## What it will be\n\nA SQLite-first database plugin for Pyxle:\n\n- A single `Database` instance shared across loaders and actions, registered through the [plugin system](../guides/plugins.md).\n- Connection pooling and PRAGMA hardening tuned for the Pyxle request lifecycle.\n- Filesystem-driven migrations (`migrations/0001-init.sql`, `0002-…`) with checksum tracking so a committed migration can't be silently edited.\n- A `get_database()` shortcut so app code stays terse:\n\n  ```python\n  from pyxle_db import get_database\n\n  @server\n  async def load(request):\n      db = get_database()\n      row = await db.fetchone(\"SELECT id, title FROM posts WHERE id = ?\", (request.path_params[\"id\"],))\n      return {\"post\": dict(row) if row else None}\n  ```\n\n## Status\n\n- **Source**: internal repo — not public yet.\n- **PyPI**: not published.\n- **ETA**: very soon. Subscribe on [pyxle.dev](https://pyxle.dev) to be notified when it lands.\n\n## See also\n\n- [Plugins guide](../guides/plugins.md) — plugin system overview.\n- [Plugins API reference](../reference/plugins-api.md) — the API third-party plugins build on today.\n","prev":{"title":"Plugins API Reference","path":"reference/plugins-api"},"next":{"title":"pyxle-auth","path":"plugins/pyxle-auth"}}